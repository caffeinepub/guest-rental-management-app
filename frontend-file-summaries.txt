{
  "frontend/src/index.css": "Global stylesheet with a darkened warm earthy OKLCH color palette — light mode backgrounds and surfaces are noticeably darker (tan/brown tones instead of near-white cream), while preserving the terracotta primary, warm sand secondary, and amber accent identity; both light and dark mode tokens updated consistently.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom font families (Nunito, Playfair Display), card shadow utilities, additional border radius tokens, and a fade-in animation.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — guest CRUD, notification management, owner PIN mutations, plus three new hooks: `useGetMonthlyBillNotifications` (fetches monthly bills by month/year), `useGenerateMonthlyBills` (triggers backend bill generation), and `useMarkNotificationAsCleared` (marks a monthly bill notification as cleared/paid).",
  "frontend/src/components/Layout.tsx": "App shell with sticky header (logo, nav links), responsive mobile menu, main content area, and footer with attribution and caffeine.ai link.",
  "frontend/src/components/GuestForm.tsx": "Reusable Dialog-based form for adding or editing guest profiles with validation, error display, and loading states for both add and edit modes.",
  "frontend/src/components/GuestCard.tsx": "Card component displaying individual guest info (name, ID, phone, room, rent, due date, payment status) with edit (opens GuestForm) and remove (with AlertDialog confirmation) actions.",
  "frontend/src/components/NotificationList.tsx": "Displays active and cleared notifications grouped by status; active notifications show guest details and a \"Mark Paid\" button that calls clearNotification.",
  "frontend/src/pages/OwnerDashboard.tsx": "Owner Dashboard page with PIN gate, now featuring a fourth stats card for monthly pending bills, a new \"Monthly Bills\" tab showing `MonthlyBillNotificationList` for the current month/year, and auto-generation of monthly bills via `useGenerateMonthlyBills` triggered on authentication and refresh.",
  "frontend/src/pages/GuestView.tsx": "Guest bill lookup page that now shows a dedicated \"Monthly Bill Notifications\" card using `GuestBillNotificationList` after a successful guest ID lookup, plus a separate \"Other Notifications\" section for legacy non-monthly notifications.",
  "frontend/src/pages/Home.tsx": "Landing/home page with hero section, feature highlights, and quick-start card linking to Owner Dashboard and Guest View.",
  "frontend/src/App.tsx": "TanStack Router setup with root route (Layout wrapper), home (/), owner dashboard (/owner), and guest view (/guest) routes.",
  "frontend/src/components/OwnerPinPrompt.tsx": "PIN entry prompt component rendered before the Owner Dashboard; shows a password input with show/hide toggle, calls useValidateOwnerPin on submit, displays an error on wrong PIN, and invokes onSuccess callback when the PIN is correct.",
  "frontend/src/components/MonthlyBillNotificationList.tsx": "New owner-facing component that displays monthly bill notifications split into active (pending) and cleared sections, with guest name, room, rent amount, due date, and billing month shown on each card; includes a \"Mark Paid\" button using `useMarkNotificationAsCleared` and a loading skeleton state.",
  "frontend/src/components/GuestBillNotificationList.tsx": "New read-only guest-facing component that displays a guest's monthly bill notifications (filtered to billingMonth > 0) split into pending and paid sections, showing billing month/year, rent amount, and due date with status badges; no action buttons since guests cannot clear their own bills."
}